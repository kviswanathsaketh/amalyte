// Shared component library — every component here is consumed by all five apps.
// If two properties need the same thing, it belongs here, not duplicated
// per-app. See spec §9 governance.
export { TopStrip } from './TopStrip';
export { Button } from './Button';
export { Card } from './Card';
export { Input, Textarea, Label } from './Form';
export { Footer } from './Footer';
export { CaseStudyShell } from './CaseStudyShell';
